# test file
# this is my first piece of code written in VScode on my mac
# im doing this while waiting for my phone screen to get fixed at the apple store


#print("My phone screen is not responding, so I'm going to have to get it repaired so it functions properly again.")

import random

import numpy as np

from numpy import doc

final_guitar_list = []
used_list = []
guitar_list = ['gibson', 'fender', 'prs', 'epiphone', 'taylor']

#print(help(np.doc.basics))
#print(help(np.doc.ufuncs))

#print(np.range(10))

#for i, guitar in enumerate(guitar_list):
#    print(np)
#    print(np.randi(1,10))